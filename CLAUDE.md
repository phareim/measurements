# Measurements

Flutter mobile app for tracking measurements.

## Tech Stack

- Flutter (SDK ^3.10.7), Dart
- Material Design, Cupertino Icons

## Commands

- `flutter pub get` — install dependencies
- `flutter run` — run on connected device/emulator
- `flutter build` — production build
